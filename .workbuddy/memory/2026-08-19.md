# 2026-08-19

## 修复 fetch.py 抓取全失败 + Verge 订阅校验失败死循环
- 根因链：0 节点 → list.meta.yml 的 group[13] 空 proxies → Verge 校验失败内核不启动 → fetch 走代理全 "connection refused" → 0 节点。
- 修复：
  - `sources.list`：BX-JD 行末 `#` 注释改为整行注释（行末注释触发 ValueError 崩溃）。
  - `fetch.py`：0 节点时分组兜底 `['DIRECT']`；`clash_data` 增加 REALITY tls 兜底、fingerprint→client-fingerprint、hysteria2 obfs:none 清理；`supports_meta` 丢弃缺 cipher 的 vmess/ss、缺 obfs-password 的 ssr；`isfake` 防 server=None 崩溃。
  - 产物 1690 节点，`verge-mihomo -t` 校验 test is successful。
- 同步：`cp list.meta.yml` 覆盖 `%APPDATA%/.../profiles/LciaAAmThknc.yaml`（旧版备份 LciaAAmThknc.yaml.bak.20260819）；需在 Verge 重新启用本地订阅 list.meta.yml。
- 发现：直连 GitHub raw 当前可用；Verge 运行时端口为 7890（非旧记忆的 7897）；内核在 `D:\1_software\clash\clash_verge_rev\Clash Verge\verge-mihomo.exe`。

## 新增面板前端（serve.py + web/dashboard.html）
- 参照 SakuraCat / 云鸟面板风格，用纯标准库 HTTP + 单页 HTML 实现。
- `serve.py`：端口 8088，路由 `/`(面板) / `/api/status`(JSON 统计) / `/api/update`(触发 fetch) / `/api/update/status`(轮询) / `/list.meta.yml`(订阅分发) / `/snippets/*` / `/list_result.csv`。
- `web/dashboard.html`：深色卡片风格，含统计卡片(总节点/地区/源/更新时间)、订阅链接一键复制、客户端下载网格、地区节点分布、在线更新按钮(带 spinner 轮询)、源抓取详情列表。
- 启动：`.venv/Scripts/python.exe serve.py`，访问 `http://127.0.0.1:8088`。
- 修复 CSV 解析：总计行 `row[0]=='总计'` 而非 `row[1]`。

## 纯静态部署方案（GitHub Pages 免费部署）
- 目标：作为面试作品展示前端面板，订阅定时自动更新，无需在线触发更新。
- 新增 `gen_status.py`：读取 list_result.csv / snippets/*.meta.yml / list.meta.yml 首行注释，生成 `web/status.json`（静态 JSON，供前端 fetch）。
- 改造 `web/dashboard.html`：`/api/status` 改为 `fetch('status.json')`；移除在线更新按钮和 `/api/update` 相关代码；新增"自动更新机制"说明卡片（GitHub Actions 每小时）；订阅链接 `getSubUrl()` 根据 github.io 域名判断指向 raw.githubusercontent.com 还是同站路径。
- 更新 `.github/workflows/fetch.yml`：增加 `python gen_status.py` 步骤；`git add web/status.json` 加入 commit；push 触发路径增加 `gen_status.py` 和 `web/`。
- 新增 `DEPLOY.md`：GitHub Pages 开启步骤、验证方法、面试展示要点。
- 本地验证：gen_status.py 生成 1697 节点 / 41 源 / 13 地区，status.json 正确。本地 http.server 8090 预览通过。
- 部署方式：GitHub Pages Source 设为 `master / /web` 分支目录，访问 `https://<用户名>.github.io/AutoMergePublicNodes/`。

## 品牌改造为 Royin Proxy + 推送到新仓库
- 用户新仓库：`origin` = `https://github.com/royin237-cpu/royin-s-proxy.git`；`master` remote = gitee 旧镜像。
- README 全文重写为 Royin Proxy（保留一行二次开发来源声明 + Anti 996 License 链接，许可证要求）；删掉原版公告/题外话/badges。
- dashboard.html：标题改 Royin Proxy；footer 改"基于 NoMoreWalls 二次开发"；`REPO_OWNER/REPO_NAME` 改 royin237-cpu/royin-s-proxy。
- DEPLOY.md 链接全部替换为新仓库地址。
- **修复 fetch.yml 死循环隐患**：之前加的 push 触发路径 `web/` 会因 workflow 自身 commit `web/status.json` 后再次触发 workflow → 无限循环，已移除（保留 `gen_status.py` 触发）。
- 本地 commit `9c35afb`（README/DEPLOY/dashboard/fetch.yml 4 文件）；**push origin 失败**：环境无 gh CLI、Windows 凭据管理器无 github 凭据，需用户在自己终端执行 `git push origin master`。
- 待办：push 后到仓库 Settings → Pages 开启（master / /web）。
- 新仓库状态（API 查询）：public、非 fork、default_branch=master、**has_pages=false**；远程已含 gen_status.py/web/DEPLOY（用户自推），仅差本地 commit `9c35afb` 未推。
- 环境无 gh CLI、无 GitHub 凭据（Windows 凭据管理器无 github 条目）→ push 需用户配合。
- 已给用户两条路：① 粘贴 fine-grained PAT（仅 royin-s-proxy 仓库，Contents+Pages 读写，用完即 revoke）由我 API 完成 push+开 Pages+验证；② 自己终端 `git push origin master` + UI 开 Pages。
