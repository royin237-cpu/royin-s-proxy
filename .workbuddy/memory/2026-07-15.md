# 2026-07-15 工作记录

## 项目还原到远程
- 用户要求把项目还原到远程 `master/master`（提交 `d656da0`，两次 "q" 提交代码主体一致）。
- 操作：`git reset --hard master/master` + `git clean -fdx`。验证了 64 个 tracked 文件全部存在、git status 干净、fetch.py/sources.list 与远程 diff 为空。
- 注意：误删了被 git 跟踪的 `.idea/` 下 7 个文件，已用 `git checkout -- .idea/` 恢复。
- `.venv/` 被 clean 删掉（不在 git）。`.workbuddy/` 是唯一保留的非 git 目录（AI 记忆）。

## 跑通 fetch.py
- 重建 `.venv/`（`C:\Users\Royin\.workbuddy\binaries\python\versions\3.13.12\python.exe -m venv .venv`），装依赖 PyYAML==6.0.1 / requests==2.28.2 / requests-file（来自 github.com peasoft，已验证 github 可达）。
- 初次直连跑 fetch.py：节点抓不到（GitHub raw 直连超时），生成 `proxies: &id001 []` 空数组。

## 修复两个导致 Verge 加载报错的问题
1. **config.yml 含废弃字段 `global-client-fingerprint: chrome`**：新版 mihomo/Clash Verge 已移除顶层该字段，加载时报 "global-client-fingerprint configuration is removed"。已从 config.yml 删除（fetch.py 只读取不写回，模板无则产物无）。
2. **节点抓取全部失败（0 节点）**：根因是之前还原时删了 `local_proxy.conf`，fetch.py 直连 GitHub raw 超时。重建 `local_proxy.conf`（内容 `http://127.0.0.1:7897`）后重跑，抓到 113 个节点，`✅ 手动选择` 等分组通过 `*id001` 锚点引用真实节点，Verge 不再报 "proxies missing"。

## 关键约定（长期）
- Verge 实际代理端口是 **7897**（config.yml 里 `mixed-port: 7890` 是订阅生成模板，非 Verge 运行时端口）。
- fetch.py 必须走 7897 代理才能抓 GitHub raw 源：需 `local_proxy.conf` 存在且内容为 `http://127.0.0.1:7897`。
- 远程仓库在 gitee，当前网络 `git fetch` 认证失败（terminal prompts disabled），但本地 `remotes/master/master` 引用即最新 `d656da0`。
